seed: 0
algorithm: bptt

task:
  batch_size: 16
  copy_length: 5

model:
  hidden_size: 64

training:
  num_steps: 500
  learning_rate: 0.001
  log_every: 50

logging:
  save_csv: true
  output_path: results/logs/copy_bptt.csv
